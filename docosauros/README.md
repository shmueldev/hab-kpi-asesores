# docosauros

Documentación de **HAB KPI Asesores** con Docusaurus y el branding del tablero (navy `#061953`, acento `#0b31a5`, highlight `#8bc7f7`).

```bash
cd docosauros
npm start
```

http://localhost:3000

```bash
npm run build
npm run serve
```

No subas `node_modules/`, `build/` ni `.docusaurus/`.
